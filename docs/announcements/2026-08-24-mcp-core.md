# launch draft: MCP core

the MCP surface now has a small stable core: analyze, verify, fact lint, and final check. Hosts can ask the server for its declared capabilities instead of guessing which tool is safe to call.

everything stays local. this is a draft for approval, not a publication instruction.
