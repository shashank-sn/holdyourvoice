# Cursor MCP setup

Configure a stdio MCP command of `hyv mcp` in Cursor after installing `@holdyourvoice/hyv`. Verify the connection with `hyv_mcp_capabilities`, then use the core tools for local analysis and final output gating. Run `hyv_final_check` on the exact final text after every later edit.
